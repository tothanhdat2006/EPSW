replicaCount: 2

image:
  repository: dvc/ingestion-service
  tag: latest

service:
  port: 3001

env:
  PORT: "3001"
  NODE_ENV: production
  LOG_LEVEL: info
  MAX_FILE_SIZE_MB: "50"

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
